/* A vendored spec header (PKCS#11-style): names must resolve as
   references without being documented. */
#define CKA_TEST 0x102
#define _CKA_INTERNAL 1

typedef struct CK_THING {
   int member;
} CK_THING;
